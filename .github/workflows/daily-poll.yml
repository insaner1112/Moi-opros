name: Daily Game Poll

on:
  schedule:
    # Каждый день в 12:00 UTC+4 (08:00 UTC)
    - cron: '0 8 * * *'
  workflow_dispatch: # Разрешает ручной запуск

jobs:
  send-poll:
    runs-on: ubuntu-latest
    
    steps:
    - name: Send Telegram Poll
      run: |
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendPoll" \
          -H "Content-Type: application/json" \
          -d '{
            "chat_id": "${{ secrets.CHAT_ID }}",
            "question": "🎮 Играем сегодня?",
            "options": ["✅ ДА", "❌ НЕТ"],
            "is_anonymous": false,
            "type": "regular",
            "allows_multiple_answers": false
          }'
